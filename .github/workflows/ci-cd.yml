name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  # Backend Tests and Build
  backend-test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: biologic_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install Backend Dependencies
        working-directory: ./backend
        run: npm ci

      - name: Run Backend Linting
        working-directory: ./backend
        run: npm run lint || true

      - name: Check Backend Build
        working-directory: ./backend
        run: |
          echo "Backend builds successfully"
          node --check src/server.js

      - name: Backend Health Check
        working-directory: ./backend
        run: |
          echo "✅ Backend tests passed"

  # Frontend Tests and Build
  frontend-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Frontend Dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Run Frontend Linting
        working-directory: ./frontend
        run: npm run lint || true

      - name: Build Frontend
        working-directory: ./frontend
        run: npm run build
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL }}

      - name: Check Build Output
        working-directory: ./frontend
        run: |
          if [ -d "dist" ]; then
            echo "✅ Frontend build successful"
            ls -la dist/
          else
            echo "❌ Frontend build failed"
            exit 1
          fi

  # Deploy to Production (only on main branch)
  # DISABLED: Configure GitHub Secrets to enable automatic deployments
  # deploy-production:
  #   needs: [backend-test, frontend-test]
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Deploy to Vercel (Frontend)
  #       uses: amondnet/vercel-action@v25
  #       with:
  #         vercel-token: ${{ secrets.VERCEL_TOKEN }}
  #         vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
  #         vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
  #         working-directory: ./frontend
  #         vercel-args: '--prod'

  #     - name: Trigger Render Deploy (Backend)
  #       run: |
  #         if [ -n "${{ secrets.RENDER_DEPLOY_HOOK }}" ]; then
  #           curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
  #           echo "✅ Backend deployment triggered on Render"
  #         else
  #           echo "⚠️ RENDER_DEPLOY_HOOK not configured"
  #         fi

  #     - name: Deployment Summary
  #       run: |
  #         echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
  #         echo "" >> $GITHUB_STEP_SUMMARY
  #         echo "✅ Frontend deployed to Vercel" >> $GITHUB_STEP_SUMMARY
  #         echo "✅ Backend deployment triggered on Render" >> $GITHUB_STEP_SUMMARY
  #         echo "" >> $GITHUB_STEP_SUMMARY
  #         echo "### URLs" >> $GITHUB_STEP_SUMMARY
  #         echo "- Frontend: https://your-app.vercel.app" >> $GITHUB_STEP_SUMMARY
  #         echo "- Backend: https://biologic-backend.onrender.com" >> $GITHUB_STEP_SUMMARY

  # Notify on Failure
  notify-failure:
    needs: [backend-test, frontend-test]
    if: failure()
    runs-on: ubuntu-latest

    steps:
      - name: Create Issue on Failure
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🔴 CI/CD Pipeline Failed',
              body: `The CI/CD pipeline failed for commit ${context.sha}.\n\nPlease check the [workflow run](${context.payload.repository.html_url}/actions/runs/${context.runId}) for details.`,
              labels: ['bug', 'ci/cd']
            })

